# project_id é preenchido localmente por `supabase link --project-ref SEU_PROJECT_REF`.
# Não commite o ref real do seu projeto Supabase neste arquivo.

[functions.books]
verify_jwt = true

[functions.library]
verify_jwt = true