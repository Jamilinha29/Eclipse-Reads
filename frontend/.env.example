# =============================================================================
# Eclipse Reads — variáveis do frontend (Vite)
# =============================================================================
# Local: copie este arquivo para .env na pasta frontend e preencha.
# Vercel: Project → Settings → Environment Variables
#   - Use os MESMOS nomes (prefixo VITE_ é obrigatório).
#   - Marque Production (e Preview, se quiser builds de branch).
#   - Após criar/editar variáveis: faça um Redeploy (o Vite embute VITE_* no build).
# =============================================================================

# --- Obrigatórias (sem elas o app quebra ao carregar o cliente Supabase) ---
# URL do projeto: Supabase Dashboard → Project Settings → API → Project URL
VITE_SUPABASE_URL=https://xxxxxxxx.supabase.co

# Chave pública (anon / publishable): mesma tela → anon public key
# Nome no código: VITE_SUPABASE_PUBLISHABLE_KEY (não use VITE_SUPABASE_ANON_KEY)
#
# SEGURANÇA (DevTools → Network):
# - Header `apikey` com JWT curto = NORMAL (é esta chave anon; proteção vem do RLS).
# - Header `Accept-Profile: public` = NORMAL (schema do Postgres).
# - NUNCA coloque service_role / SUPABASE_SERVICE_KEY no frontend (sem prefixo VITE_ também).
# - A service_role fica só em services/backend/envs/*.env (gitignored) e na Vercel (serverless).
VITE_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# --- Backends HTTP (livros + biblioteca/perfil) — opcional em dev/produção ---
# Se omitidas, o app usa /api/books e /api/library (proxy Vite local; serverless na Vercel).
# Defina URLs absolutas só se os backends estiverem em outro domínio.

# VITE_BOOKS_API_URL=http://localhost:4000
# VITE_LIBRARY_API_URL=http://localhost:4200
# VITE_API_URL=https://api.seudominio.com
